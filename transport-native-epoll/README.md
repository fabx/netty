# Building Native Transport

To build this native transport you need to use Linux with kernel 2.6 or higher.

Beside this you need the following libraries installed:
 * gcc-multilib
 * libtool
 * automake
 * autoconf 
